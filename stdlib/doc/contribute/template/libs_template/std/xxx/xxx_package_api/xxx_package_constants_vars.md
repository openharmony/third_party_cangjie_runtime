# 常量&变量

> **说明：**
>
> 1. 按 **常量(const)、不可变变量(let)、可变变量(var)** 的类型顺序展示。
> 2. const类型、let类型、var类型的API内部按 **字母顺序** 排列。
> 3. 在文档中提到其他的API都需要使用超链接的方式链过去，例：
>
>    ```text
>    [ArrayList](./xx/xxx.md#class-arraylist)
>    ```

## const xx

【必写】（定义声明）

```cangjie
public const xx: dtype = xxxx
```

【必写】功能：xxx。

【必写】类型：[dtype](link)

## const xxx

同上

## let xx

同上

## let xxx

同上

## var xx

同上

## var xxx

同上
